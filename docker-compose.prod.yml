version: '3.8'

services:
  backend:
    image: ${DOCKER_HUB_ID:-your-id}/quanty_trade-backend:${BACKEND_VERSION:-dev}
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/backend/data
    environment:
      - PORT=8080
      - DB_DRIVER=sqlite
    restart: always

  frontend:
    image: ${DOCKER_HUB_ID:-your-id}/quanty_trade-frontend:${FRONTEND_VERSION:-dev}
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
