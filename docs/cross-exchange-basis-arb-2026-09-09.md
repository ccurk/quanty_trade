# 跨所价差套利(binance/gate 永续)—— 实测 + 可回测规则(台账 #14)

- 调查人:老徐(策略组)
- 日期:2026-09-09
- 台账 #14 原文:`未探索:跨所价差套利(binance/gate 同币价差已实测有系统性基差,16/20 合约候选 |基差|>半价差)`
- **口径时刻**:实时盘口 = `2026-09-09T03:09:04Z → 03:56:13Z`(45 轮 / 60 秒一轮);
  历史序列 = 该时刻回溯 **1000 小时(约 41.7 天)**。**数字不带时刻会腐烂,这条从 #7 学的。**
- 红线遵守:全程只读**公开、无鉴权**端点(gate `/api/v4/futures/usdt/*`、binance `/fapi/v1/*`);
  未读任何 `conf_*.yaml` / `backend.env`,未 `docker inspect`,未碰任何 key/secret;
  **未下单、未改任何交易配置**;服务器只跑了两条只读的 `ls`/`find`;未 push 任何远端。

---

## 0. 一句话结论

**基差是真的、而且是系统性的 —— 但正因为它系统性,它不是套利。**

41 天小时级实测:67 个流动性达标合约里,只有 **5 个** 的 |基差中位| > 5 bps 且同号率 ≥90%
(BNB −10.74bps/100%、BZ −9.28/100%、CL −7.38/99%、SKHY −6.95/97%、UAI −17.20/92%)。
台账说的"系统性"成立。

但**系统性偏移 = 不收敛**。不收敛的价差没法靠"买便宜的所、卖贵的所、等它回来"赚钱 ——
你进场吃的那 14 bps,出场时原样吐回去。真正能落袋的只有**持有期的资金费差**,
而资金费差的 29 天实测是 **CL +0.50 bps/天、BZ +0.16、BNB +0.33,同号率 59%**
—— 抵不过双吃单来回 25 bps 的成本,回本要 50~80 天,而符号只有 59% 稳定。

**所以口径要改:这条线不是"跨所价差套利",是 funding carry 的跨所版本 ——
而且和台账 #22 的结论一样,机制真、钱假。**

唯一还活着的版本是**收敛交易**(在基差偏离自身分布的极端处进、回中位出),它的死结是一个硬事实:

> **41 天振幅(p90−p10)> 25 bps 的 11 个合约里,四个 L1 档都 ≥$2k 的:0 个。
> 反过来,L1 深度都 ≥$2k 的 24 个合约,振幅中位只有 8.5 bps。**
> **有空间的没量,有量的没空间** —— 和 #15 在做市侧撞到的是同一堵墙。

**天花板已实测(§9,规则 v0 真跑在 41 天序列上,再乘我们真实的钱)**:

> 最现实的一格(不事后挑币 + 双挂单悲观 8bps + 真实的 $57.60 永续钱包)= **−$3 ~ −$14 / 年,是负的**。
> 绝对天花板 **$163.74/年**,需要四重乐观同时成立(事后挑币 + 2bps 费率 + 把推断的 $236 全搬进永续 + 3x)。
> **低的原因是资金不是机会**:同一次模拟的年化**率**在乐观口径下是 **+41.6%** ——
> 41.6% 不是坏生意,**$36/年 才是**。分母是 $57.60。

**裁决:不作为独立玩法启动。** 但**该收的数据必须现在开始收**(§5),
因为把它变成可回测只差"落库 + 两个字段",而这批数据同时是 #15 的地基(§6)。

**所以走 Y(§9.4 详述)**:① **先拿两所账号级费率** —— §9.2 那张表从 −180% 到 +41.6% 的
全部跨度都是费率造成的,这是唯一"一次登录就能消除、却决定整条线正负号"的未知数,
应从待办 #6 升成 #14/#15 的最高优先级前置;② 采集器落库今天开始(与资金无关,30 天后才有样本外);
③ 把 #14 当"等资金到位再启用的容器" —— 做到 $1,000/年需名义 $2,404,
即**保证金 $1,603(3x 口径)~ $7,212(§4.5 安全闸口径)**,
比 #22 的 $21,700 低 3~13 倍,**两条 carry 玩法里 #14 资金效率更高**。

**另有一条比 #14 本身更紧急的副产品发现(影响当前实盘做市,不是 #14)**:
`internal/marketmaker/gate.go`(312 行)在 gate 下单路径上**没有任何限流器/重试/退避**
(grep 命中 0),而该账号正处于 #84 惩罚档、实测首次拒单率 25%+。详见 §4.7 ③ / §9.4。

---

## 1. 先纠正台账原文的两处口径

### 1.1 "16/20" 是什么

台账 #14 和 #15 都写了"16",但**是两个不同的 16**,我把小唐 9/08 的原始候选表
(`state/strategy/gate-futures-candidates-20260908-raw.json`,20 行)逐行重算:

| 判据 | 实测 |
|---|---|
| `|basis| > bspr`(bspr = gate 自己的半价差) | **16/20** ← #14 说的这个 |
| `|basis| > spr`(整个价差) | 14/20 |
| `net > 0`(净空间为正) | **16/20** ← #15 说的那个 |

**两个 16 是巧合,不是同一件事。** 引用时别互相当证据。

### 1.2 这个判据本身几乎是恒真的,不能当机会的证据

判据 `|基差| > 半价差` 出自 `config.go:56`(`|b|>s/2` 那条注释),它回答的是**做市时两条腿会不会同时挂错边**,
和"跨所套利有没有钱赚"没有关系。我这轮 75 个合约的实测:

- **判据 B(|基差中位| > gate 半价差):73/75** —— 几乎全中
- 判据 A(|基差中位| > 两所半价差之和):58/75

原因很简单:gate 价差中位只有 **1.26 bps**(半价差 0.63),binance **1.20 bps**,
而 |基差| 中位 **4.16 bps**。**价差窄到这个程度,判据基本恒成立。**
所以"16/20 都超了"不是信号强,是尺子太短。**#14 不能建立在这个判据上。**

---

## 2. 实测(要求 1):我这次自己测的,不是照抄

### 2.1 方法

三条采样链路并行,全部公开端点、零鉴权(脚本在 `state/strategy/xbasis/`,见 §5.4):

| 链路 | 端点 | 频率 × 轮数 |
|---|---|---|
| 盘口 | gate `/futures/usdt/tickers`(全量)+ binance `/fapi/v1/ticker/bookTicker`(全量),**两个 curl 并发起** | 60s × 45 |
| 前置校验 | 16 个合约的 gate `/futures/usdt/order_book?with_id=true`(带 `update` 时间戳) | 60s × 35 |
| 指数/资金费 | binance `/fapi/v1/premiumIndex`(markPrice / indexPrice / lastFundingRate) | 60s × 35 |

宇宙:gate 全部 990 个 USDT 永续 → 24h 成交额 ≥ $5M 且 binance 有同名永续 → **75 个**
(与 `backend/internal/marketmaker/gate_futures_scan.go` 的 `futScanMinQuoteVolUSD` 对齐)。
额外加了一道**单位错配闸**:两所中价比值不在 [0.9, 1.1] 内的整条丢弃(防 1000X 之类的口径不同源)。
本轮实测 0 个被这条丢掉。

**采样时只落原始八个数,所有 bps 离线算**(台账 #7 的教训:只落派生量,复核时分不清是数据错还是公式错)。
两所抓取跨度实测中位 **3,565ms**、p90 **5,841ms** —— 这段错位是本节所有数字的噪声下限,写在这里不藏。

### 2.2 前置校验:gate 的 tickers 端点陈旧吗(必须先答,否则后面全废)

信号只有个位数 bps,如果 `tickers` 是缓存的,量到的就是**数据延迟**不是**价格差**。
小唐 9/08 的表、仓库里的 `gate_futures_scan.go`、我这轮的采样,**全都建在 tickers 上**。
所以我对 16 个合约同时拉了 `tickers` 和 `order_book`,35 轮:

| 结果 | 值 |
|---|---|
| 用 tickers 量的基差 − 用 order_book 量的基差,**中位** | **0.00 bps**(16 个合约里 **11 个恰为 0.00**;非零的 5 个:ZHIPU −3.43、AKE −0.94、VVV +0.80、OPENAI −0.46、FF +0.34) |
| `|tk_mid − ob_mid|` 中位 | 0.00 ~ 6.89 bps(逐合约) |
| `|tk_mid − ob_mid|` **p90** | 最大 **24.94 bps**(VVV_USDT)、22.32(AKE_USDT) |
| order_book 服务端 `current − update` 中位 | 0.055s |

**结论:tickers 在中位上不偏,可以用来【扫描】;但尾部能偏 20+ bps,绝不能用来【触发下单】。**
下单前必须以 `order_book` 复核 —— 这条直接写进 §4 的规则里,也是给 `gate_futures_scan.go` 的一条修正建议(§5.2)。

### 2.3 快照结果(45 轮窗口,**不是时间序列**)

**先说清楚这一节能证明什么:它只能说明"此刻的横截面长什么样",它不能证明任何"系统性"。**
47 分钟对"系统性"这个词毫无发言权 —— 那是 §2.4 的活。

全样本(n=75):

| 指标 | 中位 |
|---|---|
| \|基差\| | **4.16 bps** |
| gate 价差 | 1.26 bps |
| binance 价差 | 1.20 bps |
| \|指数差\|(两所各自指数之差) | 1.47 bps |
| \|溢价差\|(两所各自"价格−自家指数"之差) | 2.86 bps |

净空间(**毛空间** = 最优方向的 `对手价 − 对手价`,未扣费):

| 门槛 | 命中 |
|---|---|
| 毛空间 > 12.5 bps(双吃单成本,见 §2.5) | **7/75** |
| 毛空间 > 4.0 bps(gate 挂单 + binance 吃单) | 25/75 |
| 四个 L1 档都 ≥ $2k | 25/75 |
| **同时满足"毛空间 > 12.5" 和 "四档都 ≥$2k"** | **2/75**(CL、BZ) |

**毛空间最大的 10 个(2026-09-09T03:09→03:56Z,45 轮中位):**

| contract | 基差 | p10 | p90 | 同号率 | gate价差 | bn价差 | **毛空间** | 净@双吃 | L1最小档 $ | gate资费/8h | bn资费/8h | 资费差 | 指数差 | 溢价差 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BZ_USDT | −28.41 | −30.43 | −26.99 | 100% | 1.02 | 1.01 | **+27.40** | +14.90 | 43,171 | −20.72 | −13.82 | −6.90 | −22.06 | −6.74 |
| CL_USDT | −27.83 | −30.92 | −26.71 | 100% | 1.07 | 1.07 | **+26.76** | +14.26 | 90,944 | −25.23 | −15.16 | −10.07 | −19.79 | −8.18 |
| OPENAI_USDT | −25.92 | −30.10 | −21.30 | 100% | 1.78 | 4.47 | +22.10 | +9.60 | 106 | 0.00 | +0.50 | −0.50 | −25.61 | +1.46 |
| FF_USDT | +22.11 | +15.72 | +32.74 | 100% | 1.36 | 1.36 | +20.41 | +7.91 | 77 | +15.28 | +1.00 | +14.28 | −1.40 | +23.02 |
| ZHIPU_USDT | +21.50 | +15.78 | +25.32 | 100% | 0.86 | 3.43 | +18.90 | +6.40 | 103 | +7.18 | 0.00 | +7.18 | +13.08 | +8.41 |
| CXMT_USDT | −21.14 | −24.68 | −17.95 | 100% | 1.18 | 3.52 | +18.80 | +6.30 | 335 | −13.12 | −36.54 | +23.42 | −23.13 | +0.87 |
| VVV_USDT | −19.27 | −32.04 | −9.77 | 98% | 3.22 | 1.99 | +16.90 | +4.40 | 94 | +1.00 | +1.81 | −0.81 | +6.64 | −25.80 |
| BNB_USDT | −12.41 | −13.52 | −10.90 | 100% | 0.67 | 0.13 | +12.01 | −0.49 | 4,022 | +0.29 | +0.09 | +0.20 | −0.00 | −11.85 |
| PONS_USDT | −14.54 | −24.01 | −6.41 | 98% | 3.95 | 2.63 | +11.73 | −0.77 | 134 | +6.46 | +6.04 | +0.42 | −36.84 | +9.04 |
| AKE_USDT | +13.18 | −2.50 | +18.83 | 89% | 1.89 | 0.65 | +11.63 | −0.87 | 63 | −36.70 | −40.69 | +3.99 | +7.85 | +4.50 |

> 单位:全部 bps。"基差" = (gate中价 − binance中价)/binance中价。
> "L1最小档 $" = 四个 L1 档(两所各自买一/卖一)折成美元后的**最小值** —— 跨所两条腿要同时打得动,
> 能做多大由最薄的那一档决定,不是由最厚的。
> "净@双吃" = 毛空间 − 12.5 bps(gate taker 7.5 + binance taker 5,**两个都未用账号核实**,见 §7)。

**这张表最重要的一行不是 CL,是 BNB**:$4,022 的最薄档、0.67bps 的价差、100% 同号、
**指数差恰好 0.00** —— 也就是说 BNB 的 12.4 bps 全是溢价差,不是两所指数口径不同。
它是这批里唯一一个"干净的、深的、稳的"跨所偏移。记住它,§2.4 还会再见到。

### 2.4 41 天历史 —— 这一节才有资格谈"系统性"

盘口深度只能实时采,但**两所的 K 线都开放历史**,所以基差本身可以拉出时间序列:

```
gate  /api/v4/futures/usdt/candlesticks?contract=X&interval=1h&limit=1000        合约成交价
gate  /api/v4/futures/usdt/candlesticks?contract=index_X&interval=1h&limit=1000  gate 自家指数
bnce  /fapi/v1/klines?symbol=X&interval=1h&limit=1000                           合约成交价
bnce  /fapi/v1/indexPriceKlines?pair=X&interval=1h&limit=1000                    binance 自家指数
```

**口径警告(必须一起读)**:K 线收盘价是**最后一笔成交价**,不是中价。所以这里的基差混了
两边各自的买卖盘抖动,量级约半个价差(两所价差中位都约 1.2~1.3 bps,即约 ±0.7 bps 噪声)。
**对 10 bps 级别的基差不影响结论;对 2 bps 级别的别看。**

75 个候选跑完,67 个有 ≥500 小时重叠。**|基差中位| 的中位数只有 2.32 bps。**

**同号率 ≥90% 且 |基差中位| >5bps 的,只有 5 个:**

| contract | 小时数 | 基差中位 | p10 | p90 | **振幅 p90−p10** | std | 同号率 | 指数差 | 溢价差 |
|---|---|---|---|---|---|---|---|---|---|
| **BNB_USDT** | 1000 | **−10.74** | −13.12 | −7.93 | **5.19** | 2.39 | **100%** | **+0.00** | −10.74 |
| BZ_USDT | 1000 | −9.28 | −16.33 | −5.48 | 10.84 | 5.81 | 100% | −6.95 | −1.67 |
| CL_USDT | 1000 | −7.38 | −14.27 | −3.61 | 10.66 | 6.83 | 99% | −5.25 | −2.04 |
| SKHY_USDT | 1000 | −6.95 | −11.16 | −2.75 | 8.42 | 3.55 | 97% | −1.81 | −5.18 |
| UAI_USDT | 1000 | −17.20 | −32.26 | −1.25 | 31.01 | 12.06 | 92% | +0.40 | −17.78 |

**对照组(说明这套测量没坏)**:BTC 基差中位 −0.42 bps(std 0.96)、ETH −0.21(1.06)、
SOL −0.94(1.93)、XRP −0.69(1.55)。**主流币两所几乎不存在基差** —— 这正是应该的,
也说明上面那 5 个不是测量噪声。

**同时注意 OPENAI_USDT**:今日快照 −25.92 bps 看着很像机会,41 天历史却是
**中位 +0.21、std 186.27、p10 −280、同号率 52%** —— 它不是基差,是噪声。
**这就是"单次快照不能证明系统性"最贵的一个例子:今天排第三的候选,历史上是个纯随机数。**

### 2.5 台账原文的定性:成立,但推不出机会

台账写"已实测有系统性基差" —— **成立**(BNB 41 天 100% 同号)。
但"系统性"恰恰是它不能被套利的原因,理由在 §3。

---

## 3. 这个基差为什么存在、会不会被吃掉(要求 2)

### 3.1 分解:基差 = 指数差 + 溢价差

恒等式(实测逐行成立):

```
basis = (gate价 − bn价)/bn价
      = [(gate价 − gate指数)/gate指数] − [(bn价 − bn指数)/bn指数] + (gate指数 − bn指数)/bn指数
      =            gate溢价           −           bn溢价          +        指数差
```

全样本实测(45 轮):**|指数差| 中位 1.47 bps、|溢价差| 中位 2.86 bps、|基差| 中位 4.16 bps。**
逐个看,两种成因都真实存在,而且**不同合约主导项完全不同**:

| 主导项 | 例子(41 天历史) | 含义 |
|---|---|---|
| **指数差** | BZ(基差 −9.28,其中指数差 −6.95,占 75%)、CL(−7.38 / −5.25,71%)、OPENAI(今日 −25.92 / −25.61,99%) | **两所对同一个标的的"公允价"算得就不一样**(成分交易所、取样方式不同)。这不是错价,是两个不同的标尺。**它没有任何理由收敛。** |
| **溢价差** | **BNB(基差 −10.74,指数差 +0.00,溢价差 −10.74)**、UAI(−17.20 / +0.40 / −17.78) | 两所指数一致,**是 gate 的永续本身持续贴水**。这是真的供需差,理论上被资金费拉住。 |

**这条分解直接否掉了"跨所价差套利"这个说法的一半**:凡是指数差主导的(BZ/CL/OPENAI),
你买的和卖的**根本不是同一个价格口径**,把它当同一标的对冲,你持有的是"两个指数之差"这个风险,
不是无风险价差。

### 3.2 那它会不会被资金费吃掉 —— 资金费差实测

**先摆机制**:两个永续都没有到期日,**没有任何机制强制两所价格收敛**。
唯一把仓位变成现金的通道是资金费:持有 [gate 多 / binance 空] 的净收 = −gate费 + binance费。

**截面相关性(45 轮 × 75 合约,同一时刻)**:

- corr(基差, 资金费差/8h) = **0.257**
- corr(**溢价差**, 资金费差/8h) = **0.467**,斜率 0.742

**溢价差和资金费差的相关明显高于基差和资金费差** —— 和 §3.1 的分解一致:
资金费只对"价格 vs 自家指数"负责,对"两家指数谁高谁低"根本不负责。**机制自洽。**

**但截面相关性不是钱。真正决定收益的是资金费差的【时间序列】。**
两所的历史资金费也是公开的(`/fapi/v1/fundingRate`、`/futures/usdt/funding_rate`),
按 **UTC 自然日把当日全部结算加总再相减**(这样自动吃掉两所结算频率不同的问题 ——
实测 binance 多为 4h、gate 多为 8h,CXMT 甚至是 gate 1h vs bn 8h),29 天重叠窗口:

| contract | 重叠天数 | gate 日均 | bn 日均 | **[多gate/空bn] 日均净收** | 同号率 | **29天累计** |
|---|---|---|---|---|---|---|
| CL_USDT | 29 | −4.42 | −1.71 | **+0.50** | 59% | **−7** |
| BZ_USDT | 29 | −5.63 | −3.43 | **+0.16** | 59% | **−11** |
| BNB_USDT | 29 | +0.91 | +0.76 | **+0.33** | 59% | +9 |
| OPENAI_USDT | 29 | +0.03 | +1.50 | **+1.47** | **100%** | **+43** |
| VVV_USDT | 29 | +3.00 | +3.00 | +1.11 | 79% | +66 |
| AKE_USDT | 29 | +3.00 | +5.49 | +2.04 | 62% | +4 |
| XAUT_USDT | 29 | 0.00 | +1.67 | +1.38 | 83% | +32 |
| PAXG_USDT | 29 | 0.00 | +1.17 | +1.28 | 86% | +29 |
| BTC_USDT | 29 | +1.50 | +2.09 | +0.66 | 83% | +20 |
| CXMT_USDT | 22 | −12.27 | −26.25 | **−5.07** | 64% | **−142** |
| SOPH_USDT | 29 | +3.00 | +3.00 | −0.00 | 93% | **−626** |

> 单位 bps。日均取中位数,累计取算术和。

**这张表是本报告最贵的一张,因为它推翻了快照。**

快照那一刻 CL 的资金费差是 **−10.07 bps/8h**,折年化像是 +116%/年的白捡钱。
拉到 29 天:**日均只有 +0.50 bps,同号率 59%(等于抛硬币),累计 −7 bps(是负的)。**
**快照上的巨额 carry 是瞬时极值,不是收入。** 只用快照做这条线,会把一个抛硬币的东西
当成年化三位数的机会 —— 这正是台账 #14 卡住的地方(小唐 9/08 也只有一次快照)。

最好的一个是 OPENAI:**+1.47 bps/天、29 天 100% 同号、累计 +43 bps**。
但 OPENAI 的基差本身 std 是 186 bps(§2.4),**carry 是稳的,腿上的价却在随机游走** ——
这两件事必须分开看,不能因为 carry 稳就说这条腿能拿。

### 3.3 口径裁决

**#14 的正确口径是:跨所 funding carry,不是跨所价差套利。**

理由三条,全部实测:
1. 两个永续都不到期,**不存在强制收敛机制**(机制事实);
2. 基差里 |指数差| 中位 1.47 bps、且在最大的几个候选上占 71%~99%,
   **指数差没有理由收敛**(§3.1);
3. 唯一的现金流是资金费差,而它 29 天日均 +0.16 ~ +0.50 bps、同号率 59%(§3.2)。

**和台账 #22(资金费率套利)是同一个家族。#22 的裁决是"机制真、钱假";
这条的裁决是"机制真、钱更假"** —— 因为 #22 只需一个所的资金费为正,
这条要**两个所的资金费之差**为正且稳定,而差值的信噪比比单边低得多。

### 3.4 会不会被吃掉:三个吃掉它的通道

| 通道 | 实测/状态 |
|---|---|
| **手续费** | 双吃单来回 **25 bps**(进 12.5 + 出 12.5)。相对 §2.4 的振幅中位 8.5 bps(深池)—— **成本是信号的 3 倍**。费率本身未核实(§7)。 |
| **资金费** | 见 §3.2。既是唯一收入,也是最大不确定项。 |
| **划转/提现通道** | 跨所两条腿要各自备保证金,腿子跑偏要挪钱。仓库现状:`internal/rebalance/` **只覆盖两所的现货余额**(`balance_source.go:34` 打 gate `/api/v4/spot/accounts`、`:85` 打 binance `/api/v3/account`),挪钱走 `withdraw.go` 的**链上提现**。**永续钱包不在里面**,而 spot↔futures 内部划转两所都需要单独调用(已无凭据探测:gate `POST /api/v4/wallet/transfers` 返回 `MISSING_REQUIRED_HEADER`、binance `POST /sapi/v1/futures/transfer` 返回 `-2014 API-key format invalid`,**都是"存在且需鉴权",不是 404**)。**提现费率与到账时间未测**(需鉴权),标为未知成本。 |

---

## 4. 压成可回测规则(要求 3)

### 4.1 先说清楚:现在**回测不了**,缺的是数据不是脑子

要回测跨所进出,至少要有**两所对齐的盘口时间序列(含深度)**。仓库里没有,服务器上也没有 ——
我实测过:`gate-futures-scan.jsonl` 在本机和 mycloud 上**都不存在**(`find / -name "gate-futures-scan*"` 无结果),
即 `gate_futures_scan.go`(commit `449da94`)**写好了但没部署**。
所以下面给的是**规则和参数,不是回测结果**。规则的每个阈值都指明"用哪个字段、怎么算",
数据一到就能直接跑 —— 这是 §5 存在的理由。

### 4.2 规则 v0(建议只在 §4.6 的"影子模式"下跑)

**标的池(每日一次离线重算)**,四条同时满足:

```
① 41天 |基差中位| ≥ 5 bps            (来源:两所 1h K线,§2.4 的算法)
② 41天 同号率 ≥ 90%                   (剔除 OPENAI 这种纯噪声)
③ 41天 振幅 p90−p10 ≥ 3 × 来回成本    (来回成本见 §4.4;双吃单口径 = 75 bps 门槛)
④ 四个 L1 档美元深度中位 ≥ 5 × 单笔名义
```

**实测:今天这个池子是空的。**③ 和 ④ 互斥 —— 振幅 >25bps 的 11 个合约里,
L1 四档都 ≥$2k 的 **0 个**;L1 四档都 ≥$2k 的 24 个合约,振幅中位 **8.5 bps**。
**把这条写成规则的意义正在于:它自己会说"今天没得做",而不是逼人硬选一个。**

**进场**(z 值口径,不用绝对 bps —— 绝对阈值在 41 天里会被基差自身的漂移废掉):

```
b_t   = (gate中价 − bn中价)/bn中价 × 1e4          ← 必须来自 order_book,不是 tickers(§2.2)
μ, σ  = b 在过去 7 天(168 根 1h)的中位数与标准差
z_t   = (b_t − μ) / σ

进场条件(全部满足):
  |z_t| ≥ 2.0
  且 该方向毛空间 > 来回成本 × 1.5                  ← 毛空间用【对手价】算,不是中价
  且 两所 L1 该侧美元深度 ≥ 3 × 单笔名义
  且 两所 order_book 的 update 时间戳与本地时钟差 < 2s
  且 距离任一所的下一个资金费结算 > 10 分钟          ← 结算点前后基差会跳
方向:z_t < 0 → gate 便宜 → 多 gate / 空 bn;z_t > 0 反之
挂单方式:两条腿都【挂单】(gate poc / binance GTX),见 §4.3 —— 吃单腿只用于"救腿"
```

**出场**(先到先执行):

```
① z_t 回到 |z| ≤ 0.3                    → 正常平仓(两腿同时挂单平)
② 持仓 > 48 小时                        → 无条件平(carry 抵不过占用,§3.2)
③ |z_t| ≥ 4.0 且方向不利                → 止损平(基差不收敛,认错)
④ 单腿净敞口 > 单笔名义 × 5%,持续 > 30s  → 立刻市价补平那一腿(§4.3)
⑤ 任一所 order_book 断流 > 5s            → 全平,不等
```

### 4.3 单腿失败 —— 跨所套利的头号杀手,规则必须先写这条

**这是唯一一条我建议"宁可少赚也要照做"的:**

```
两腿都用挂单进场,但【永远先挂流动性差的那一腿】,成交后才挂另一腿。
  ├─ 先挂腿(慢腿)= 两所里 L1 该侧美元深度更小的那个所
  ├─ 慢腿成交(或部分成交)→ 立刻用【吃单】在快腿补等量对冲
  │    这一步不许挂单、不许等价格 —— 对冲的价值高于那 5~7 bps 的吃单费
  └─ 慢腿未成交 → T+15s 撤单,本轮作废(不追价)

净敞口闸(硬闸,fail-closed):
  |gate仓位 + bn仓位|(折基础币)> 单笔名义 × 5%,持续 >30s
    → 市价补平较小的那一腿,并【暂停该 symbol 30 分钟】
  连续 3 次触发 → 停整个策略,等人看

进程级:
  两所都必须挂死人开关(countdown_cancel_all)。gate 合约版已实测存在
  (见 #15 评估 §3.4);binance 侧对应 /fapi/v1/countdownCancelAll —— 未测。
  死人开关只撤单不平仓 —— 这是 deadman.go:37 的已知缺口(#15 §4 的缺口②),
  跨所场景下它更致命:撤了单但两条腿还在,进程死了就是裸奔的方向性仓位。
  **在补上"崩溃后自动平仓"之前,这条线不许上真钱。**
```

### 4.4 成本口径(每个数都标了来源)

| 项 | 值 | 来源 |
|---|---|---|
| gate 合约 taker | 7.5 bps | `/futures/usdt/contracts` `taker_fee_rate=0.00075`,988/988 一致。**合约级字段,不是账号级** |
| gate 合约 maker | −1.0 bps | 同上 `maker_fee_rate=-0.0001`。**与 gate 官方公告 36485 的 VIP0 +2bps 冲突**,见 §7 |
| binance U 本位 taker | **5.0 bps(估计,未实测)** | VIP0 公开档。`https://www.binance.com/en/fee/futureFee` 实拉需登录,页面返回 "No records found",**拿不到** |
| binance U 本位 maker | **2.0 bps(估计,未实测)** | 同上 |
| **来回成本(双吃单)** | **25 bps** | (7.5+5) × 2 |
| **来回成本(双挂单,乐观)** | **2 bps** | (−1+2) × 2 |
| **来回成本(双挂单,悲观)** | **8 bps** | (2+2) × 2 |

**费率符号直接决定生意成不成立** —— 和 #15 的结论一字不差。
双挂单悲观口径 8 bps 下,深池 24 个合约里振幅 >8bps 的有 **14 个**
(CL、BZ、SKHY、SKHYNIX、TRUMP、SAMSUNG、DRAM、XAG、ONDO、SOXL、WLD、XAU、SNDK、ADA)。
**这是这条线唯一还活着的版本** —— 但它要求两条腿都挂上、都成交,也就是 §4.3 里最难的那件事。

### 4.5 仓位上限

```
单笔名义        ≤ min(四个 L1 档美元深度) / 5          实测约束:BNB $4,022/5 ≈ $800
单 symbol 名义   ≤ 单笔 × 3
全策略总名义     ≤ 两所永续钱包可用保证金之和 / 3      ← 3 倍安全垫,不是杠杆
杠杆            两所都显式 setLeverage(≤3),不许用账号默认档
                (#15 §4 缺口⑥:代码里从没设置过杠杆)
同时持仓 symbol  ≤ 2                                   基差之间不独立(§3.1 指数差是共因)
```

**资金硬约束(来自台账 #22 §5,不是我测的)**:所有者可见的永续钱包余额 **$57.60**。
按上面的闸,单笔上限会被资金而不是深度卡死。
最小可行票实测:binance `MIN_NOTIONAL` 多数 5 USDT(BTC 50、ETH 20),
gate 一张合约名义 CL $0.93 / BNB $0.75 / OPENAI $15.18 —— **物理上开得出来,但规模没有意义**。
按 §3.2 最好的 OPENAI(+1.47bps/天),$1,000 名义一个月毛收约 **$4.4**,
减去 25 bps 进出($2.5),**约 $1.9/月** —— 与 #22 的量级完全一致。

### 4.6 影子模式(建议的唯一下一步)

规则写好后**不接下单**,只做三件事:记录每次"本应进场"的时刻、当时两所 order_book 快照、
以及此后 48 小时的基差路径。跑满 30 天,拿真实的进场分布回答两个问题:
① 慢腿在 15 秒内的成交率是多少;② 进场后 z 真的会回到 0.3 吗。
**这两个问题现在一个都答不了,而它们决定这条线的全部收益。**

### 4.7 gate 限流(台账 #84)怎么吃进规则里 —— 本节是 v0 漏掉的

台账 #84 是小唐 2026-09-08 21:00 的实测:gate 账号被压在**成交率惩罚档 10 请求/10 秒(UID 级)**,
≤8 笔零拒单、=10 笔拒单率 25%+;`FR = 成交额/(下单+改单+撤单)额` 实测 **0.018~0.037**,
阈值 0.1;解限需成交额 ≥2850(只算下单)USD/天,今天约 1048。
**"跨所套利要两条腿同时成、gate 这条腿被限到 1 笔/秒" 听起来是结构性死刑 —— 实测下来不是,
但真正的杀招在另一个地方。** 三条分开算(脚本 `ceiling.py` §D):

**① 吞吐:不卡。差 2,725 倍。**

规则 v0 在 41 天上实跑,资金共享、同时最多 2 个仓,可成交 **220 次往返 → 5.29 次/天**。
gate 侧每往返约 6 个请求(进场挂慢腿 1 + 可能撤 1 + 补快腿 1;出场同),
即 **31.7 请求/天**,而惩罚档预算是 1 笔/秒 × 86400 = **86,400 请求/天**,占用 **0.037%**。
**#14 的瓶颈从来不是限流,是资金(§9)。** 这条要写进规则,是为了防止有人拿 #84 当借口砍掉整条线。

**② 对小唐 FR 的外部性:当前资金下可忽略,但符号未知。**

今日分母 ≈ 1048 / 0.0275 ≈ **38,109 USD/天**(**推导值,不是实测** —— 用 #84 的 FR 中值倒推)。
#14 新增的挂单额:真实钱 $57.60 按 §4.5 闸是 **101 USD/天(分母的 0.27%)**,
放到 3x 是 **457 USD/天(1.20%)**。
注意 **#14 的成交也进分子**,所以它是抬高还是压低 FR,取决于 `sign(#14 自己的成交率 − 0.0275)`
—— 而 #14 的慢腿成交率**至今未测**(§7 第 4 条)。**结论:量级 ≤1.2%,当前可忽略;
但资金放大 100 倍后这条会翻脸,届时必须走独立 UID/子账号,不与做市共用那把 key。**

**③ 真正的杀招:救腿请求抢不到桶,而 gate 下单路径上没有任何重试。**

做市引擎 `refresh_ms=1000`(`internal/marketmaker/config.go:42`,实测)→ 每秒撤 1 + 挂 1
≈ **2 请求/秒的需求 vs 1 请求/秒的配额**,**桶长期是空的** —— 这正是 #84 量到 25%+ 拒单率的成因。
而 §4.3 的净敞口硬闸要求 **30 秒内市价补平单腿**。理论上 30 秒能重试 30 次,失败概率 0.25ⁿ,本该没事。

> **但实测:`internal/marketmaker/gate.go`(312 行)里
> `grep -E 'ratelimit|429|TOO_MANY|limiter|Retry|backoff|TooMany'` 命中 0。
> gate 下单路径上既没有限流器,也没有任何重试/退避。被拒就是被拒。**
>
> 对照组:`internal/exchange/binance.go` 有 `rateLimitMu` / `usedWeight1m` / 429 本地熔断
> (`binance.go:90-212`)。**即 binance 那条腿有,gate 这条腿没有 —— 而被惩罚限流的恰恰是 gate。**

**后果**:单腿成交后要补对冲,请求被拒 → 没有重试 → 补不上 → **裸奔的方向性仓位**。
这不是概率事件,是 25% 的首次拒单率乘以"零重试"。

**因此规则 v0 增补三条硬约束(全部 fail-closed):**

```
R1  gate 下单路径必须先有【重试 + 退避】,且救腿(补对冲/平仓)请求的优先级
    高于做市 requote。在这条落地之前,#14 不许接真钱 —— 与 §4.3 的 deadman
    缺口并列,是同一类"崩溃即裸奔"。
R2  #14 与做市不得共用同一把 gate key(独立 UID 或子账号)。
    理由不是吞吐(富余 2725 倍),是【隔离救腿请求的配额】,顺带消掉 ② 的 FR 外部性。
R3  进场前检查 gate 桶余量:若最近 10 秒已发 ≥8 个请求(#84 实测的零拒单边界),
    本轮不进场。宁可漏掉机会,也不要进场后补不上腿。
```

**R1/R2 都不是 #14 独有的** —— #15 同样跑在这把被惩罚的 key 上,同样零重试。
**这是本轮的一个副产品发现,建议单独立项(详见 §9.4)。**

---

## 5. 数据规格(要求 4):要哪些字段、什么频率、存哪

### 5.1 好消息:采集器已经写好了,只是没部署、缺两个字段

`backend/internal/marketmaker/gate_futures_scan.go`(359 行,commit `449da94`)
已经在采 8 个原始报价 + 张数换算 + 四个 L1 美元深度 + gate 指数 + gate 资金费,
**正是 #14 要的东西**。它现在的问题只有三个:

| 问题 | 修法 |
|---|---|
| ① **binance 侧的指数和资金费没采**(它自己的注释也承认"binance 侧资金费本轮没有采") | 每轮多打一次 `GET /fapi/v1/premiumIndex`(全量 900 行、一次请求),取 `indexPrice` / `markPrice` / `lastFundingRate` / `nextFundingTime` 四个字段。**§3.1 的分解没有这四个字段就做不了** |
| ② **只写 JSONL,50MB×5 轮转 ≈ 保留 3.3 天** | 回测要 30 天以上,**必须落库**(§5.3) |
| ③ 盘口取自 `tickers`,尾部能偏 20+ bps(§2.2) | 扫描保持 `tickers`(便宜);**候选池(≤20 个)另开一路 `order_book?with_id=true`**,把 `update` 时间戳一起落。**没有时间戳就没法证明这一行不是陈旧数据** |

### 5.2 字段清单(在现有 `FuturesScanRow` 上增补)

现有字段全保留(它已经守住了"原始数必须够重算所有派生量"这条)。**新增 8 个:**

| 字段 | 来源 | 为什么必须有 |
|---|---|---|
| `ref_index_px` | bn `premiumIndex.indexPrice` | §3.1 指数差,**分解的一半** |
| `ref_mark_px` | bn `premiumIndex.markPrice` | 强平计算用的是 mark,不是中价 |
| `ref_funding_bps` | bn `premiumIndex.lastFundingRate` | §3.2 carry 的另一半 |
| `ref_funding_interval_h` | bn `/fapi/v1/fundingInfo.fundingIntervalHours` | **两所结算频率不同(实测 bn 多为 4h、gate 多为 8h、CXMT 是 1h vs 8h)。不存这个字段,日化/年化一定算错** |
| `exec_funding_interval_s` | gate `/futures/usdt/contracts.funding_interval` | 同上 |
| `exec_next_funding_ts` / `ref_next_funding_ts` | 两所各自 | §4.2 的"结算点前后 10 分钟不进场" |
| `exec_book_update_ts` | gate `order_book?with_id=true` 的 `update` | §2.2:**证明这一行不是陈旧数据** |

**采集频率**:扫描池 60s/轮(维持现状);候选池(≤20 个)**5s/轮**。
理由:§4.2 的进场条件带 15 秒级的挂单窗口,60 秒粒度测不出慢腿成交率。

### 5.3 存哪

**MySQL,照 markout 的现成模式做**,不要再发明一套:
`markout_sink.go`(258 行)+ `markoutdb/store.go`(237 行)+ `scripts/markout_persistence.sql`(283 行)
已经把"非阻塞投递 → 批量写 → 建表 DDL"这条路走通了(commit `c6cf781`)。

- 表 1 `xvenue_scan`:扫描池,60s × 75 合约 ≈ **10.8 万行/天**。留 90 天 ≈ 972 万行。
- 表 2 `xvenue_book`:候选池,5s × 20 合约 ≈ **34.6 万行/天**。留 30 天 ≈ 1,038 万行。
- 两张表都按 `(contract, ts)` 建索引,按 `ts` 分区,**留存策略写进 DDL**
  —— 台账 #37 的教训:没有轮转/留存的东西迟早把 40G 盘写满。

### 5.4 本轮脚本的去处

本轮脚本与原始数据已归档到 **agent-office 仓库** `state/strategy/xbasis/`
(注意:不在 quanty_trade 里 —— quanty_trade 是公开仓库,本轮只往它放这一篇文档):

```
采样:  xbasis_sample.py   ob_check.py   prem_sample.py
分析:  final.py   analyze2.py   funding_hist.py   hist_basis.py
原始:  xbasis-20260909T0309Z.jsonl.gz   (45 轮 × 75 合约,3,386 行)
        obcheck-20260909T0313Z.jsonl.gz  (35 轮 × 16 合约,560 行)
        final-20260909T0356Z.json        (§2.3 那张表的全部字段)
        funding_hist-20260909.json       (§3.2 的 29 天资金费)
        hist_basis-20260909.json.gz      (§2.4 的 1000 小时基差序列,含逐点)
```

**任何人可以用同样的命令复现本文每一个数字**,派生量全部在分析脚本里、不在采样脚本里。
其中 `hist_basis.py` 和 `funding_hist.py` **不需要任何新工程就能跑** ——
它们只打公开历史端点,是这条线目前唯一免费的时间序列来源。

### 5.5 到"可回测"要多少工程量

**以下全部标注「估计,未实测」**,但每一条都锚在仓库里一个**已完成的同形工作**上:

| 阶段 | 内容 | 估计 | 锚点(实测) |
|---|---|---|---|
| A. 补字段 | `gate_futures_scan.go` 加 8 个字段 + 一次 `premiumIndex` 调用 + 候选池 order_book 路 | **约 150~250 行,1 个文件 + 单测** | 现文件 359 行,同形改动 |
| B. 落库 | 新增 sink + store + DDL + models | **约 1,600 行 / 9 个文件** | `c6cf781` 实测 **9 文件 +1,638 行**(markout 落库,同一形状) |
| C. 部署 + 攒数据 | 上 mycloud、跑满 30 天 | 代码量 ~0,**时间 30 天** | 部署仪式已有(`server_deploy_backend.sh`) |
| D. 回测器 | 读库、按 §4.2 的规则重放、出成交率/收益分布 | **约 400~700 行** | `basis_replay_test.go` 345 行(949k 条日志离线回放,`f6d2c47`)是现成模板 |

**合计估计:约 2,200~2,600 行代码 + 30 天等待。**
**其中 A+B(约 1,800 行)就是"开始攒数据"的全部成本,应该现在做 —— 因为 C 的 30 天是纯等待,越晚开始越晚有答案。**
D 可以等数据攒到再写。

---

## 6. #14 和 #15 共用哪些前置工程(必答)

**结论:共用大头,但不是全部;而且共用的那部分正好是最贵的那部分。应该合并投入,顺序是 #15 先。**

| 前置件 | #15(gate 合约做市) | #14(跨所) | 现状(实测) |
|---|---|---|---|
| **gate 合约 exec 适配器**(8 个接口方法:张数↔基础币、有符号 size、`/futures/usdt/positions`…) | **必需** | **必需** | **不存在**。已注册的 exec 只有 hyperliquid / coinsph / mexc / gate(现货)/ kucoin(`grep RegisterExec`)。#15 评估 §3.2 已给出逐行改动清单 |
| **`PerpRiskControls` 四方法**(ClosePosition / MarginRatio / SetLeverage / FundingPaidUSD) | **必需** | **必需**(而且更急:两条腿两个爆仓点) | **今天没有任何适配器实现**(`types.go` 注释,已核) |
| **合约版 deadman**(`/futures/usdt/countdown_cancel_all`) | 必需 | **必需 ×2** | gate 端点已实测存在;`deadman.go:37` 仍写死 `/spot/` |
| **合约版费率源** | 必需 | 必需 | `fee_provider.go:83` 打 `/spot/fee`;`:31` 的 gate 默认值注释自称 `NOT authoritative` |
| **合约版 PnL / 资金费入账** | 必需 | 必需 | `pnl_tracker.go` 是现货现金流模型,不含资金费 |
| **跨所观测数据流** | 有用(候选表) | **必需**(唯一地基) | `gate_futures_scan.go` 写好、**未部署**、缺 binance 指数/资金费 |
| **binance 合约 exec 适配器**(带凭据下单) | 不需要 | **必需** | **不存在**。`binance_feed.go` 只是**现货只读行情**(注释原文:"Binance SPOT … Spot, not USDM") |
| **两腿撮合状态机 + 净敞口闸**(§4.3) | 不需要 | **必需** | **不存在**。`grep -rni "hedge" internal/` **命中 0**。引擎是单场馆报价器,没有任何"两条腿"的概念 |
| **永续钱包划转**(spot↔futures、跨所挪保证金) | 单所,弱需求 | **必需** | `rebalance/` 只覆盖两所**现货**余额 + 链上提现 |

**怎么读这张表:**

- **前 6 行是共用的**,而且是工程量的主体。改造范围我今天重测了一遍(#15 评估写于 9/08,包已经长大):
  `wc -l internal/marketmaker/*.go` = **7,982 行**(#15 评估当时是 4,640);
  真正 `import` 这个包的**包外文件仍然只有 2 个**(`internal/app/runtime.go`、`internal/api/mm_observe.go`
  —— `markoutdb/` 在包内,不算)。**包在长大,爆炸半径没变,这一点仍然成立。**
  逐行改动清单沿用 #15 评估 §3.2(约 20 处修改点 + 1 个新文件),我本轮没有逐条重验。
  **#14 想动,必须先把 #15 的这 6 行做完 —— 一行都省不掉。**
- **后 3 行是 #14 独有的增量**,而且每一行都不小:一个全新的带凭据 binance 合约适配器、
  一个仓库里**完全不存在**的两腿状态机、一套跨所资金调度。
  **粗判(估计,未实测):#14 的独有增量 ≥ #15 全部工作量。**

**所以:合并投入,但顺序是 #15 先。** 做 #15 的过程会把共用的 6 件事全部落地;
做完之后再看 #14,增量是"加一个 binance 适配器 + 一个两腿状态机",届时决策成本低得多。
**反过来先做 #14 是纯亏** —— 你会为了跨所把 #15 的东西全做一遍,还额外背上两腿状态机的复杂度,
而 §2~§4 已经说明那条线现在没钱。

---

## 7. 明确未核实项(别当成已完成)

1. **账号级费率,两所都没有。** gate 合约:`/futures/usdt/contracts` 的 −1/7.5 是**合约级字段**,
   与官方公告 36485 的 VIP0(+2/+5)冲突,taker 也对不上,说明它不是我们的费率(并入待办 #6)。
   binance U 本位:公开费率页实拉需登录,**一个数都没拿到**,本文 5.0/2.0 bps 全是**估计**。
   **这两个数是 §4.4 全部结论的地基,费率符号一变结论就变。**
2. **41 天基差用的是 K 线收盘价,不是中价**,含约 ±0.7 bps 的成交价抖动噪声(§2.4 口径警告)。
3. **深度只有 L1**,没有 L2~L5。真实可打规模需要盘口全深度,`order_book?limit=20` 能拿,本轮没采。
4. **慢腿成交率未知**,§4.3 的 15 秒撤单窗口是设计参数不是实测值 —— 这是 §4.6 影子模式要答的第一个问题。
5. **提现/内部划转的费率与到账时间未测**(需鉴权)。跨所保证金调度的真实成本因此是未知数。
6. **binance `/fapi/v1/countdownCancelAll` 未探测**,不知道是否存在/行为如何。
7. **本文所有实时数字来自 47 分钟单窗口**;所有历史数字来自 1000 小时 1h K 线。
   **两者不同源,不要混着引用**(这正是 #15 评估 §2.4 那个 "0 个" 吃过的亏)。
8. 服务器上**没有**任何 `gate-futures-scan*` 文件(mycloud 上 `find / -name "gate-futures-scan*"`
   全盘实测无结果,本机同样无)—— 采集器未部署,
   所以本文之外**没有任何历史盘口数据可用**。

---

## 8. 可直接贴进日报的一句话

> 台账 #14(跨所价差套利)实测完成:41 天小时级基差显示 67 个流动性合约里仅 5 个 |基差中位|>5bps 且同号率≥90%
> (BNB −10.74bps/100%、BZ −9.28/100%、CL −7.38/99%、SKHY −6.95/97%、UAI −17.20/92%),
> 台账"系统性基差"成立;但两个永续都不到期、无强制收敛,可落袋的只有资金费差,
> 29 天实测日均仅 +0.16~+0.50 bps、同号率 59%,抵不过双吃单来回 25 bps;
> 振幅>25bps 的 11 个合约中 L1 深度≥$2k 的 **0 个**,深度达标的 24 个振幅中位仅 8.5bps ——
> **有空间的没量、有量的没空间,不作为独立玩法启动**;
> 口径改为 funding carry,与 #15 共用 6 项前置工程(合约适配器/永续风控/deadman/费率源/PnL/观测流),
> **建议先做 #15**,并立即补齐采集器的 binance 指数与资金费字段并落库(估计约 1,800 行,未实测)。

---

## 9. 天花板:按我们真实的钱,这条线一年能赚多少(要求 4)

前面 §1~§8 回答了"机会在不在";本节回答所有者真正问的那个问题:**能赚多少。**
**方法不是估,是把 §4.2 的规则 v0 真跑在 41 天序列上,再乘以我们真实拿得出的名义。**
脚本:`state/strategy/xbasis/ceiling.py`(agent-office 仓库),可复现,输入是本轮实测的两个文件。

### 9.1 回测口径(四条都会【高估】,请连同结论一起读)

| # | 口径 | 方向 |
|---|---|---|
| ① | 序列是 1h K 线**收盘成交价**之差,不是 order_book 中价之差,混入两边各半个价差的噪声(实测两所价差中位约 1.3bps) | **高估**:z 的尖峰有一部分是噪声,"回归"被算成了利润 |
| ② | 假设进出场都在小时收盘价成交 —— 100% 成交率、零滑点、零排队。§4.3 的慢腿 15 秒成交率**未测**,这里当 1 | **高估** |
| ③ | 不含资金费(§3.2 实测同号率 59%、日均 ±0.5bps,当噪声) | 中性偏乐观 |
| ④ | 41 天单窗口,**没有样本外** | **高估** |

**所以下面每个数都是乐观上界,真实值只会更低。** 这正是它的用途:**连乐观上界都不够看,就不必再讨论。**

**组合模拟口径**:资金是**共享**的 —— 同时最多 2 个仓(§4.5 "同时持仓 symbol ≤2"),
每个仓吃总名义的一半,按进场时间贪心占坑,**占不到坑的机会直接丢弃**(41 天里丢掉 407 次、只做成 220 次)。
只用 **24 个深度达标合约**(L1 四档都 ≥$2k)。

两种选池方式必须分开看:
- **`mode=all`** —— 24 个**全做**,不事后挑。**这是唯一能当样本外近似的数。**
- **`mode=cherry`** —— 事后只留 41 天净 >0 的。**有前视偏差(look-ahead),只能当乐观上界。**

### 9.2 年化收益率(占名义的百分比)

| 成本情景 | mode=all | mode=cherry(前视偏差) |
|---|---:|---:|
| 双吃单 25 bps | **−180.2%** | 0(池为空,一个都选不出来) |
| 双挂单-悲观 8 bps | **−16.2%** | +9.7% |
| 双挂单-乐观 2 bps | **+41.6%** | +46.3% |

**读法:这张表的全部信息量在"符号会翻"。**
从 25 → 8 → 2 bps,结论从"巨亏"翻到"年化 41%",而**这三个数之间的差别全是费率**。
**而两所账号级费率我们至今一个都没拿到**(§7 第 1 条,待办 #6)。

### 9.3 天花板(美元/年)

`= 年化% × 该资金情景下的总名义`。资金硬数字:永续钱包 **$57.60**(全系统唯一实测余额,#22 §5 / #42);
推断值:公司总权益约 **$236**(#42,**三口径推断,不是实测**)。

**mode=all(诚实口径):**

| 资金情景 | 总名义 | 25 bps | 8 bps | 2 bps |
|---|---:|---:|---:|---:|
| 永续钱包 $57.60 · §4.5 闸(名义=保证金/3) | $19.20 | −$34.60 | −$3.12 | **+$7.99** |
| 永续钱包 $57.60 · 3x 杠杆 | $86.40 | −$155.71 | −$14.04 | +$35.97 |
| 全部权益 $236 搬进永续 · §4.5 闸 | $78.67 | −$141.77 | −$12.78 | +$32.75 |
| 全部权益 $236 搬进永续 · 3x 杠杆 | $354.00 | −$637.99 | −$57.51 | **+$147.36** |

**mode=cherry(前视偏差,不可当预期):**

| 资金情景 | 25 bps | 8 bps | 2 bps |
|---|---:|---:|---:|
| $57.60 · §4.5 闸 | $0 | +$1.86 | +$8.88 |
| $57.60 · 3x | $0 | +$8.36 | +$39.96 |
| $236 · §4.5 闸 | $0 | +$7.61 | +$36.39 |
| $236 · 3x | $0 | +$34.25 | **+$163.74** |

### 9.4 天花板结论 —— 直说

> **最现实的那一格(mode=all + 双挂单悲观 8bps + 真实的 $57.60)是 −$3 到 −$14 一年。是负的。**
>
> **绝对天花板是 $163.74/年**,而它需要四重乐观同时成立:事后挑币(前视偏差)+ 拿到 2bps 的
> 双挂单费率 + 把**推断的**全部权益 $236 都搬进永续 + 上 3x 杠杆。**这四条现在一条都不成立。**
>
> **所以:天花板很低,而且低的原因是【资金】,不是机会本身薄。**
> 证据是同一张表的 %:最现实口径下年化率是 −16.2%,最乐观是 +41.6% ——
> **41.6% 的年化率不是一门坏生意,$36/年 才是。** 分母是 $57.60,不是收益率的问题。

**所以走 Y(否定必须带出路,三条按 ROI 排序):**

1. **【零工程、今天就能做、决定符号】先把两所账号级费率拿到手。**
   §9.2 那张表从 −180% 到 +41.6% 的全部跨度都是费率造成的。这是**唯一一个用一次登录就能消除、
   却决定整条线(以及 #15)正负号**的未知数。已在待办 #6 —— **它现在应该是 #14/#15 的最高优先级前置,
   而不是一条待办。** 需要所有者本人登录两所后台查(我不碰凭据)。
2. **【低成本、有时效】§5 的采集器落库现在就开始收。**
   30 天后才有资格谈样本外。这批数据是 #14/#15 共用地基,且成本只是"落库 + 两个字段"。
   **今天不开始收,30 天后还是今天这个局面。** 这条与资金无关,不必等钱。
3. **【定位纠正】把 #14 当成"等资金到位再启用的容器",不是丢弃。**
   它的年化率(乐观口径 +41.6%)不难看,难看的是容量。按 §9.2 的 41.6% 反推:
   **要做到 $1,000/年 需要名义 $2,404**(计算:1000/0.416),对应保证金:
   - **3x 口径**(名义 = 保证金 ÷ 2 × 3 = 保证金 × 1.5)→ 保证金 **$1,603**
   - **§4.5 安全闸口径**(名义 = 保证金 ÷ 3)→ 保证金 **$7,212**

   **这是一个明确的、可检验的复活门槛。** 与 #22 的 "$21,700 才够 $100/月" 相比,
   #14 低 **3~13 倍**(取决于口径),**在两条 carry 类玩法里 #14 的资金效率更高**。写进台账当复活条件。
   **注意这个门槛建立在 +41.6% 上,而那是 §9.1 四条高估口径叠加后的乐观值 ——
   真实门槛只会更高,不会更低。**

**副产品发现(不属于 #14,但比 #14 本身更紧急,建议单独立项):**

> **`internal/marketmaker/gate.go`(312 行)在 gate 下单路径上没有任何限流器/重试/退避
> (`grep -E 'ratelimit|429|TOO_MANY|limiter|Retry|backoff|TooMany'` 命中 0),
> 而该账号正处于 #84 的惩罚档、实测首次拒单率 25%+。**
> 这不是 #14 的未来问题 —— **做市引擎现在就跑在这把 key 上**,撤单/改单被拒同样没有重试。
> 对照 `internal/exchange/binance.go:90-212` 有完整的 429 本地熔断,**两条腿不对等**。
> 影响面是 #15 和当前实盘做市,不是 #14。详见 §4.7 ③。

### 9.5 与台账 #22 的关系(同族,别重复踩)

#22(资金费率套利)的结论是"机制真、钱假,$57.60 连一个 BTC 对都开不出来,
够到 $100/月 需要 $21,700"。**#14 是同一堵墙的另一面**:
#22 死在最小票面(开不出仓),#14 开得出仓(gate 一张 CL 名义 $0.93)但**死在容量和费率符号**。
**两条都不是"机会不存在",都是"我们的钱太少"。**
这意味着:**再查第三条 carry 类玩法的边际价值很低** —— 结论会是同一个。
**下一次调研的钱应该花在"怎么把 §9.4 第 1 条的费率拿到手",而不是找第四条玩法。**
