# v10+v11 Prompt 全文审查报告（2026-07-22）

方法：5 个审查维度并行（内部矛盾 / 平台可执行性 / 统计与交易逻辑 / 结构与激励 / 前 3 轮执行模拟）
→ 67 条原始发现 → 合并去重 14 条 → 逐条对抗验证（怀疑者立场，须引用原文反驳）。
**结果：14/14 存活，0 条误报**（confirmed 4 条，weakened-精修 10 条——缺陷全部成立，仅修正方案被精修）。
总投入 20 个代理 / ~134 万 tokens / 91 分钟。修复全部落入 `docs/cron_prompt_v12.md`。

验证过程附带实证：`quanty-ledger` 分支已上线并有多轮积累（12:19Z/13:23Z 运行在读写、
复验候选#1）→ **v11 已在调度器生效**。因此 v12 的台账条款以"升级现行结构、严禁覆盖"为前提。

## 14 条验证存活缺陷（按对资金的伤害度排序）

| # | 级 | 判定 | 缺陷 | v12 修复 |
|---|---|---|---|---|
| 1 | 高 | weakened | 恢复升档裸改 min_confidence 会把 short_thr 从 0.60 无声放宽到 0.45，当场推翻在飞 E2 实验；恢复排在读 _exp 之前、冻结清单无在飞实验项 | 门槛联动不变式（Step 3）；恢复移至 Step 5.5（在飞检查之后）；冻结清单 +在飞实验/台账前置条件/LEDGER_MISSING |
| 2 | 高 | weakened | 有持仓时 stop 被拒无处置分支：字面执行整轮 exit 1 死亡（满仓亏损时哑火成活锁）或重试洪水；且平台报错文案会诱导"主动平仓开窗"，全文无禁令 | PATCH 排队制 + 严禁为开窗平仓 + 每轮尝试 ≤2 + api_check 仅限 login/ctx + 待落队列重判定纪律（Step 2.5/执行纪律） |
| 3 | 高 | confirmed | L2 公式 round() 算出 19.80U 破 20U 用户硬边界（注释"守20U下限"自证伪）；L1 固定 0.25 在余额<80U 同样破限 | ceil2 公式 + L1 max(0.25, ceil2) + 落地前复验 余额×pct≥20 |
| 4 | 高 | weakened | E7/E8 解锁条件死锁："连续 2 轮"与"间隔 ≥6h"在 3h 节奏下互斥；且授权分级 🟢 止于 E6、🔴 禁"提频率"，同一动作两处授权矛盾 | 复现协议写死（登记轮不计数/≥6h/跌出清零/2/2 解锁）；授权分级同步 E3-E8 |
| 5 | 高 | weakened | 两份互相冲突的执行序列并存；"# 现在开始"停在文档中部且不含 v11 步骤——按它执行的轮次跳过台账，合法改动次轮被判"无主"走回滚评估 | v11 物理拼入正文；文末唯一开工令，逐字对齐 Step 7 主流程总序 |
| 6 | 高 | confirmed | 充值信号常备直令唯一存放地是 config._exp，层2 收窄会把它挤出且台账四节无收容位——用户直令丢失通道 | 台账七节制（+用户常备直令节）；_exp 收窄迁移纪律（先迁后删，push 成功才许移除） |
| 7 | 高 | confirmed | 同一实验被同时命令评判（预注册 eval_after 已到）与禁止评判（5b 默认公式 48h/500 未到），无优先级条款 | Step 5b：预注册 eval_after 契约优先，默认公式仅在未写时适用 |
| 8 | 高 | weakened | 预算三漏洞：apply 无 audit 条目+其预注册 PATCH 被 bookkeeping 豁免 → 代码改动对预算隐形；恢复升档写 LADDER: 前缀 → 次轮被当豁免类 → 6h 内可连升两级；Step 3 标题与正文预算表述矛盾 | 预算三源推导（audit + APPLY:/RECOVER: 前缀 PATCH + 台账运行日志）+ 前缀登记制 + bookkeeping 定义收紧为关账/carry |
| 9 | 高 | weakened | 两条无日期散令已过时/已被推翻（置信度动态仓位引擎已实现——验证者在 Go 代码 strategy_execution.go 实证；"不含杠杆"是被 07-20 直令推翻的旧令），新会话会当待办执行，直接改变下单尺寸 | 散令重写为带状态的常备直令快照；附录C 写明双口径（引擎含杠杆语义 vs 20U 度量不含杠杆）与 E4 封顶口径 |
| 10 | 高 | weakened | "最重要的一点"+"尽快7000万"占据最高优先级位与末行近因位，与哲学1/2/4 正面冲突且无调和规则，系统性把每 3h 的临场裁量推向扩张侧 | 哲学10 进攻观：目标实现路径=edge确认→触发器→资金阶梯→复利；HOLD=常态；降档轮的降档=最优进攻 |
| 11 | 高 | confirmed | E7 无档位门槛（L2 止血期可扩张）；白名单"冷却缩短"会砸穿 L2 固定 cooldown=1800；"择优权重"机制在 S16 代码中不存在，执行=发明新机制超出固化半径 | E7 仅 L0/L1；冷却缩短仅 L0；白名单改为 TP/SL 微调 + 择优 tie-break（非新加权机制） |
| 12 | 中 | weakened | E7 准入 8pp@n30≈0.88σ（噪音单组通过率~19%，远低于 E3 1.5σ/E6 2σ 标尺）；五维度×两侧无多重比较校正；E8 占比指标分母效应会误判劣化/误捧微亏组 | 自适应 2σ 门槛（n≥60 且 wr−be≥2√(0.25/n)）；每轮每侧至多 1 新候选；E8 指标改绝对额；eval_after 改"且"+7 天逃生阀 |
| 13 | 中 | weakened | 9 秒日志窗被当完整观测：在途订单会被记"静默失败"；"72h 落选=0"几乎必然成立会误回滚在用的 S16；熔断器 9 秒无心跳是常态却触发过渡条款双重处置；E4"或"条款单次事件即可扩容 | "未观测≠为零"铁律（附录C）；一切跨轮判定以统计行累计计数+台账观察计数增量为准；静默失败 5 分钟在途规则；E4 只认槽满增量 |
| 14 | 中 | weakened | 台账容灾失守：写路径不以读到的内容为基底（再入/引导可用空账覆盖权威）；引导链无 main 兜底；REPO_DIR 零验证；重建 grep 漏 E2fix 锚点；哲学3 未承认台账为权威源 | 写路径强制 cp 基底；禁 --force；首写 ls-remote 确认；rev-parse+remote 验证；重建=grep∪audit 并集+禁回滚窗口；哲学3 三源+audit 终裁 |

## 建议的后续人工动作

1. **把 `docs/cron_prompt_v12.md` 的 Prompt 正文整段替换调度器内容**（越早越好：#1/#2/#7 是
   现在每一轮都可能踩中的活风险；现行 quanty-ledger 候选#1 的前置条件登记只提供部分保护）。
2. 把 `LEDGER.md` 合入 main 一份（v12 引导链的第三级兜底 `origin/main:LEDGER.md` 需要它才有物可用）——
   可由本分支 PR 合并实现。
3. 充值信号已于 08:11Z 触发（TG 3137）：升档条件满足，建议按常备令充值至 250-300U；
   v12 部署后首轮会按新余额与七节台账继续。

## 免修说明（审查中被反驳掉的方向）

- "L2→L1 必然在 23:11Z 踩中门槛联动缺陷"被弱化为"预算窗开启后随时可踩"（5b 观察期
  是一道依赖执行者自觉的部分刹车）——不改变修复必要性。
- "台账再入必然静默覆盖"被弱化：远端分支存在时裸建推送会被 non-fast-forward 拒绝，
  真正的覆盖开口是执行者为满足原子纪律而 --force——v12 已明文禁止。
