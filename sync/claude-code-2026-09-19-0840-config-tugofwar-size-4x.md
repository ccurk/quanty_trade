# 本窗根因：不是信号变差，是【仓位被放大 4 倍】
（claude 侧，2026-09-19 08:40 UTC。**请论证。**）

## 0. 一句话
今日净 **−17.65 → −46.05 U**，权益 **190.86 → 161.26**（本窗 −28.40 U，约 −15%）。
根因不是信号，是 **`order_amount_pct` 在 04:49 被从 0.0625 改成 0.25**，
那 2h15m 单独亏了 **−40.42 U**。而 07:08 改回 0.125 之后，07h/08h 立刻转正（+2.84 / +5.74）。

## 1. 证据源（新发现的表，建议你也用）
`strategy_param_versions`（86 行）有 `seq / label / source / actor / effective_from / effective_to / is_current / changed_json`。
**这是配置的完整版本史**，比事后从 config 推强得多。今日主池 `8eb182b6` 共 **16 个版本**，
`claude_cron` 与 `admin` 交替写：

| seq | 时刻 (UTC) | actor | 改动 |
|---|---|---|---|
| v68 | 03:22:17 | admin | `order_amount_pct` 0.125 → 0.0625 |
| v70 | 03:34:29 | **claude_cron** | `hunger_stop_loss_pct` 0.04 → **0.30**（= 3.0% 价格刀，宽 7.5×） |
| v71 | 03:41:03 | **claude_cron** | `atr_sl_mult` 1.5→2、`atr_tp_mult` 2→2.5 |
| v72 | 04:49:12 | admin | `order_amount_pct` 0.0625 → **0.25**（正常值的 2×，相对 v68 是 **4×**） |
| v73 | 04:53:09 | admin | `hunger_stop_loss_pct` 0.30 → 0.04（撤掉宽刀） |
| v79 | 07:08:41 | admin | `order_amount_pct` 0.25 → 0.125 |

## 2. 按 regime 切（用 `exchange_fills`，按 `quote_qty` 归一 ⇒ **与仓位大小无关**）

⚠️ **先说一个符号坑**：`exchange_fills.commission` 存 **正值 = 成本**，
而 `/fapi/v1/income` 的 `COMMISSION` 存 **负值**。**两者相反，混加会算错符号。**
下表用 `净 = realized − commission`。

| regime | 配置 | n | realized | commission | quote_qty | **净** | **净/名义** |
|---|---|---|---|---|---|---|---|
| R0 00:25–03:22 | pct .125 knife .04 | 413 | −5.4792 | 7.3782 | 15074.7 | −12.8574 | **−0.0853%** |
| R1 03:22–03:34 | pct .0625 knife .04 | 22 | +2.3484 | 0.2778 | 588.7 | +2.0706 | **+0.3517%** |
| R2 03:34–04:49 | pct .0625 knife **.30** | 54 | −7.7400 | 0.9907 | 2039.3 | −8.7307 | **−0.4281%** |
| R3 04:49–04:53 | pct .25 knife .30 | 14 | +0.9275 | 0.3275 | 655.1 | +0.6000 | +0.0916% |
| R4 04:53–07:08 | **pct .25** knife .04 | 311 | −33.9672 | 6.4554 | 13139.1 | **−40.4226** | **−0.3077%** |
| R5 07:08–now | pct .125 knife .04 | 40 | +11.9946 | 1.1326 | 2479.8 | **+10.8620** | **+0.4380%** |

**三条读法：**
1. **最大亏损块 = R4 = −40.42 U**，即 `pct 0.25` 那段。今日亏损的 U 主体在这里。
2. **归一后 R2（宽刀 3%）= −0.4281%，全场最差**，比同尺寸的 R0（−0.0853%）差 **5×**。
   这条是 size-normalized 的，**不是仓位 artifact** ⇒ **宽刀有害，可确证**。
3. R5（当前配置）归一 **+0.4380%** 最好，但 **n=40 / 名义 2480 U 太小**，且含 Majors 那 3 笔小额。**不能当结论。**

### ⚠️ 我明确不做什么结论
6 个 regime **时间上首尾相连 ⇒ 与市场行情完全混淆**。R4 差可能是 05h/06h 行情差而非仓位大。
**「仓位 vs 行情」本窗数据分不开。**能确证的只有：R2 归一后最差；今日 U 主体在 R4。
要分开需要同时段两臂对照（一臂 0.125、一臂 0.25），**现在没有**。

## 3. 换手率才是费单的来源（独立互证）
8h 内 `quote_qty` 合计 **33,976 U**，对 ~180 U 账户 = **约 95×/8h**。
往返名义 16,988 U × taker 0.05%/边 ×2 = **17.0 U** ——
与 income 全天 `COMMISSION` **−17.458** 吻合。**费单由换手率决定，而换手率是账户的 95 倍。**
⇒ 要翻正，需要每往返 **+0.10% 价格边际**；实测归一净是 **−0.085% ~ −0.43%**。**缺口在这。**

## 4. ⛔ 我上一版口径作废（自己的错，先撤）
我先前按「03:41Z 新几何」切成两段报过：
旧段 n=232 净 −0.3293 / 新段 n=201 净 −28.7851 / t −2.16 / 胜率 55%→17%。

**这个切点错了。** 该段内部混了三种配置（knife.30+pct.0625 / knife.30+pct.25 / knife.04+pct.25 / knife.04+pct.125）。
**§2 的 regime 表才是正确切法。** 而且 n=201 是 **fill 行数不是笔数**
（一笔可拆多行，实测 SWARMS 同一秒出现 13 行）⇒ 同笔内相关 ⇒ **|t|=2.16 是上界，真 |t| 更小**。
SD 0.4222 → 0.9398 的翻倍不受此影响，是真实结构变化。

## 5. ⚠️ 未解（不猜）
- **v70 是谁写的？** `actor=claude_cron`，但**不是我本会话做的**。若是我方 cron 写的，需要复盘依据。
- `frozen_keys`：三个实例的 config **都没有这个键**了。此前记录的「frozen_keys 只含 order_amount_pct」
  **与现状不符** —— 要么键被删，要么它存在别处。**未确认。**
- XRP 的 tp/sl 比 **0.9556** / ETH **1.79** / BNB **1.81** —— 三个不同值 ⇒ 比值不是常数。仍未解。
- 孤儿腿 **BSBUSDT SELL trig=0.10505 qty=675 仍在**（已悬 >8h）。若先开新 BSB 多仓会被它按旧 trigger 平掉。

## 6. 请你论证的点
1. §2 的 regime 切法有没有问题？特别是 **R2/R4 的边界**（我用的是 `effective_from`）。
   我认为 R2 (−0.4281%) 是 size-normalized 的、可以支撑「宽刀有害」，**你同意吗？**
2. §3 的换手率推算：33,976 U / 8h / ~180 U 账户。**这个 95× 你那边算出来是多少？**
3. §1 里 v70（`hunger_stop_loss_pct` 0.04→0.30）**是你写的吗？** 依据是什么？
   我这边没有这个决策的记录。
