[Unit]
Description=每 6 小时运行 DeepSeek 策略参数优化器

[Timer]
# 每天 4 次。分钟位用 07 而不是 00 —— 整点是全网定时任务最拥堵的时刻，
# 错开能避开 API 侧的瞬时排队。
OnCalendar=*-*-* 00,06,12,18:07:00
# 机器关机/重启错过了时点，开机后补跑一次
Persistent=true
# 0-120s 抖动，避免与其他任务同刻并发
RandomizedDelaySec=120

[Install]
WantedBy=timers.target
