# prompt v4.0 变更清单（vs v3.2）@2026-09-14 — owner 直令"切回单策略模式+同时开10单+进攻必须交易+新prompt"
1. 核心使命：策略组模式退役→单策略（只跑 main）；"必须要交易"入使命，零成交/低于基线轮强制归因（管道死 vs 门槛高）并行动；正期望/费覆改为评判回滚指标（非前置门）。
2. 新增【频率阶梯】节：杠杆清单（lcp 0.15→0.10→0.06 / scp 0.05→0.03 / min_conf 0.55→0.52 末档 / 攒批窗 5s→3s）＋每档劣化线；基线笔/日 38.7（09-04..09-08 均值）。
3. 每轮节奏第 1 步新增【管道活性】（DNS 断连 118.6h 教训）：active 报 dial/lookup 错→TG🆘 附修复命令，禁 stop/start，排小时探测；恢复后三项复核。
4. 删除：策略组注册表/FLEET 建壳 checklist/多池路由对账器（trend/fade/breakout 池规则）/组保证金多载具公式；池对账改单池版（隔离规则原样保留＋TradFi 6 币永久隔离）。
5. 硬边界：mcp=10 固定（改须 owner）；保证金 pct×mcp≤0.75（现 0.075×10 顶格）；#6 加"严禁 ssh"；#7 加"复活退役壳/改 mcp 须问"。
6. 能力清单 O 补充：rotate 打在 starting 态=no-op（09-14 实证）；E 补充中文 q 须 URL 编码。
7. TG 报告新增 📶管道行；频率仪表改为"vs 基线 38.7＋在飞档位进度"。
8. 环境节 5 个凭证在归档版为占位符（脱敏）；可粘贴版由 Claude 经会话文件交付。
